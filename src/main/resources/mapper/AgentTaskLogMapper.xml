<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.aiframework.mapper.AgentTaskLogMapper">

    <resultMap id="LogResultMap" type="com.example.aiframework.entity.AgentTaskLogEntity">
        <id column="id" property="id"/>
        <result column="task_id" property="taskId"/>
        <result column="log_type" property="logType"/>
        <result column="content" property="content"/>
        <result column="metadata" property="metadata"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 查询任务日志 -->
    <select id="selectByTaskId" resultMap="LogResultMap">
        SELECT * FROM agent_task_logs
        WHERE task_id = #{taskId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <!-- 删除任务日志 -->
    <delete id="deleteByTaskId">
        DELETE FROM agent_task_logs
        WHERE task_id = #{taskId}
    </delete>

</mapper>
